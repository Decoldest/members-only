extends layout

block content
  h1 #{title}
  h4 Are you sure you want to delete this message?
  form(method="POST") 
    button
      a(href="/") Cancel
    button.btn.btn-primary(type='submit') Delete
