extends layout

block content
  h1= title
  p Welcome to #{title}

  p #{user}

  button 
    a(href="/sign-up") Sign Up
  if user
    button 
      a(href="/logout") Log Out 
  else 
    button 
      a(href="/login") Log In